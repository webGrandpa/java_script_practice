// Именно так!

// Теперь давай сделаем из этого многоразовый инструмент. Наша цель — написать функцию `getFirstElement`, которая будет принимать **любой** массив и возвращать его первый элемент.

// Как бы ты написал первую строчку этой функции, то есть ее объявление? Она должна называться `getFirstElement` и принимать один аргумент (сам массив), который мы можем назвать, например, `arr`.


const getFirstElement = arr => arr[0];